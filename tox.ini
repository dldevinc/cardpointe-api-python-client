[tox]
envlist =
  py{37,38,39,310,311}

[testenv]
usedevelop = true

setenv =
  PYTHONPATH={toxinidir}

deps =
  setuptools
  -r{toxinidir}/requirements.txt

commands =
  pytest --basetemp="{envtmpdir}" {posargs}
